{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "text-formatter",
  "title": "Text formatter",
  "description": "Formats the text and copies to clipboard",
  "icon": "command-icon.png",
  "author": "mustanseer_sakerwala",
  "categories": [
    "Productivity",
    "Developer Tools",
    "Data"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Replace <br> with new line",
      "subtitle": "Does what it says in title",
      "description": "It replaces <br> with new line, is needed for transforming text when copied from kanban obsidian",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.69.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "20.8.10",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}